#include <stdio.h>
#include <stdlib.h>

void destroiListaLong(linkedListLong * lista)
{
    noLong * aux1 = lista->head;
    noLong * aux2;
    while(aux != NULL){
        aux2 = aux1->prox;
        free(aux);
        aux1 = aux2;
    }
}

void criaListaLong(noLong * head){
    malloc
}


